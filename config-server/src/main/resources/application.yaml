server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Vivekwebcoder/distributed-ecommerce-microservices.git
          default-label: develop_vivek

management:
  endpoints:
    web:
      exposure:
        include: "*"
